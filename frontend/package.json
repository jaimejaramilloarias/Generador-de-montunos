{
  "name": "montuno-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:pages": "vite build --mode production",
    "test": "vitest --run",
    "test:watch": "vitest",
    "test:e2e": "npm run build -- --mode production && playwright test",
    "test:ci": "npm run test && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^4.0.9"
  },
  "dependencies": {
    "@tonejs/midi": "^2.0.28",
    "tone": "^15.1.22",
    "zod": "^4.1.12"
  }
}
